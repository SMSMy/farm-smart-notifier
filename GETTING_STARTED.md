# 🚀 البدء السريع - نظام التنبيه الذكي للمزرعة

دليل سريع للحصول على النظام يعمل في 15 دقيقة.

## ⚡ التشغيل السريع (5 دقائق)

### 1️⃣ إعداد التوكنات

انسخ `.env.example` إلى `.env` وأدخل بياناتك:

```bash
cp .env.example .env
```

عدّل ملف `.env`:

```env
TELEGRAM_BOT_TOKEN=ضع_هنا_توكن_البوت
TELEGRAM_CHAT_ID=ضع_هنا_معرف_المحادثة
OPENWEATHER_API_KEY=ضع_هنا_مفتاح_الطقس
```

### 2️⃣ اختبار سريع

```bash
python app.py test
```

إذا نجح، سترى:
```
✅ تم تحميل الإعدادات: 13 شجرة
✅ تم تهيئة Telegram بنجاح
✅ جميع الاختبارات نجحت!
```

### 3️⃣ التشغيل الأول

```bash
python app.py
```

سيتم إرسال رسائل تجريبية إذا كانت هناك مهام.

## 🔑 الحصول على التوكنات (10 دقائق)

### 📱 Telegram Bot

1. **أرسل رسالة لـ [@BotFather](https://t.me/BotFather)**:
   ```
   /newbot
   ```

2. **اتبع التعليمات**:
   - أدخل اسم البوت: `Farm Notifier`
   - أدخل اسم المستخدم: `farm_notifier_bot`

3. **احفظ التوكن** من الرسالة:
   ```
   123456789:ABCdefGHIjklMNOpqrsTUVwxyz
   ```

4. **إضافة البوت للمجموعة**:
   - أضف `@farm_notifier_bot` للمجموعة
   - أرسل رسالة للتجربة
   - احصل على Chat ID من الرسالة التالية

### 🌤️ OpenWeatherMap

1. **اذهب لـ**: https://openweathermap.org/api
2. **أنشئ حساب مجاني**
3. **انسخ API Key** من لوحة التحكم

## ✅ تحقق من الإعداد

### اختبار Telegram

```python
python telegram.py
```

يجب أن ترى:
```
✅ الاتصال ناجح مع البوت: Farm Notifier (@farm_notifier_bot)
✅ اختبار Telegram ناجح
```

### اختبار الطقس

```python
python weather.py
```

يجب أن ترى بيانات الطقس.

### اختبار الجدولة

```python
python logic.py
```

يجب أن ترى:
```
=== اختبار Farm Logic ===
✅ اليوم موعد دواء الديدان: Fenbendazole
✅ اليوم موعد تطهير الحظيرة
مهام التسميد اليوم: 3
  - banana: NPK 30-10-10
  - mango_small: NPK 20-20-20
  - pomegranate: NPK 15-15-15
```

## 🎯 تخصيص النظام

### تعديل الجدولة

في `config.json`:

```json
{
  "chicken_schedule": {
    "deworming": {
      "interval_days": 90,    // تغيير الفاصل
      "start_date": "2025-10-01"  // تاريخ البداية
    }
  },
  "trees_fertilizer_schedule": {
    "banana": {
      "amount_kg": 1.5,      // تغيير الكمية
      "notes": "أضف ملاحظاتك هنا"
    }
  }
}
```

### إضافة أشجار جديدة

```json
{
  "my_tree": {
    "times_per_year": 2,
    "seasons": ["spring_season", "autumn_season"],
    "fertilizer": "NPK 20-20-20",
    "amount_kg": 0.5,
    "notes": "ملاحظات الشجرة"
  }
}
```

وأضف الاسم في `app.py`:

```python
TREE_NAMES_MAP = {
    'my_tree': 'اسم شجرتي',
    # باقي الأشجار...
}
```

## 🤖 التشغيل التلقائي (GitHub)

### 1️⃣ رفع للمطور

```bash
git add .
git commit -m "إعداد نظام التنبيه"
git push origin main
```

### 2️⃣ إضافة Secrets

في GitHub → Settings → Secrets:

- `TELEGRAM_BOT_TOKEN`: توكن البوت
- `TELEGRAM_CHAT_ID`: معرف المحادثة  
- `OPENWEATHER_API_KEY`: مفتاح الطقس

### 3️⃣ تفعيل Actions

- اذهب لـ Actions tab
- فعّل "Farm Notifier Scheduler"
- التشغيل سيبدأ كل 12 ساعة

## 📊 مراقبة النظام

### فحص آخر تشغيل

```bash
cat .last_run
```

### تشغيل يدوي

```bash
# في GitHub Actions
workflow_dispatch

# محلياً
python app.py
```

### فحص السجلات

```bash
# تشغيل مع تفاصيل
python app.py

# فحص الأخطاء
tail -f farm_notifier.log
```

## 🆘 حل المشاكل السريع

| المشكلة | الحل السريع |
|---------|-------------|
| `YOUR_BOT_TOKEN_HERE` | حدّث ملف `.env` |
| `requests.exceptions` | تحقق من OpenWeatherMap key |
| لا توجد صور | تأكد من وجود `images/*.jpg` |
| خطأ في GitHub | تحقق من Secrets في repository |

## 📞 طلب المساعدة

- **للأخطاء**: شغّل `python app.py test` وأرسل النتيجة
- **للتطوير**: راجع `README.md` للتفاصيل
- **للاستفسارات**: استخدم وضع `python app.py help`

---

**🎉 مبروك! نظامك جاهز للعمل التلقائي!**

التشغيل التالي سيكون تلقائياً عبر GitHub Actions كل 12 ساعة. 🌱