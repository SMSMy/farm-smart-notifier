#!/usr/bin/env python3
"""
سكريبت مساعد لتسجيل تغيير غذاء الدجاج.
عند تشغيله، سيقوم النظام تلقائيًا بإرسال تنبيه فيتامينات في التشغيل المجدول التالي.
"""
from logic import FarmLogic
import sys

if __name__ == "__main__":
    print("📋 تسجيل تغيير الغذاء...")
    try:
        logic = FarmLogic()
        logic.mark_feed_changed()
        print("\n✅ تم التسجيل بنجاح.")
        print("💡 سيتم إرسال تنبيه فيتامينات تلقائيًا في التشغيل القادم للنظام.")
    except Exception as e:
        print(f"\n❌ حدث خطأ: {e}")
        sys.exit(1)
