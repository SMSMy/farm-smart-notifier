# 🌱 دليل التثبيت والتشغيل الشامل - نظام التنبيه الذكي للمزرعة

## نظرة عامة
هذا الدليل يحتوي على تعليمات خطوة بخطوة لتثبيت وتشغيل نظام التنبيه الذكي للمزرعة على الجهاز المحلي ونشره على GitHub Actions.

## 📋 المتطلبات

### المتطلبات الأساسية:
- Python 3.8+ (مُختبر على 3.12)
- Git
- حساب Telegram (للتطبيق)
- مفتاح API من OpenWeatherMap (مجاني)

---

## 🛠️ التثبيت المحلي

### الخطوة 1: تحميل المشروع
```bash
# تحميل المشروع (من مجلد farm-notifier)
cd farm-notifier

# أو نسخ الملفات يدوياً إلى مجلد جديد
mkdir my-farm-notifier
cd my-farm-notifier
```

### الخطوة 2: إنشاء ملف .env
```bash
# إنشاء ملف .env من القالب
cp .env.example .env
```

### الخطوة 3: تحديث ملف .env
قم بتحديث ملف `.env` بالمفاتيح الصحيحة:

```env
# === TELEGRAM ===
TELEGRAM_BOT_TOKEN=YOUR_BOT_TOKEN_HERE
TELEGRAM_CHAT_ID=YOUR_CHAT_ID_HERE

# === WEATHER ===
WEATHER_API_KEY=YOUR_WEATHER_API_KEY
```

---

## 📱 إعداد Telegram Bot

### الخطوة 1: إنشاء Bot عبر BotFather
1. فتح Telegram والبحث عن `@BotFather`
2. إرسال `/newbot`
3. اتباع التعليمات لإنشاء البوت
4. **مهم**: حفظ التوكن المُعطى

### الخطوة 2: الحصول على Chat ID
#### للمحادثة الشخصية:
```bash
# تشغيل البوت وإرسال رسالة
python app.py test

# ستجد خطأ "Chat not found" - هذا طبيعي في الاختبار الأول
```

#### للمجموعات:
1. إضافة البوت للمجموعة
2. إرسال رسالة في المجموعة تتضمن اسم البوت
3. استخدام أداة الحصول على Chat ID:

```python
# تشغيل هذا الكود للحصول على Chat ID
import os
from telegram import Bot
from dotenv import load_dotenv

load_dotenv()
bot = Bot(token=os.getenv('TELEGRAM_BOT_TOKEN'))

# يجب أن تكون هذه في handler للتحديثات
async def main():
    async with bot:
        updates = await bot.get_updates()
        for update:
            if update.message and update.message.chat:
                print(f"Chat ID: {update.message.chat.id}")
```

---

## 🌤️ الحصول على Weather API Key

### من OpenWeatherMap:
1. الذهاب إلى [openweathermap.org](https://openweathermap.org/api)
2. إنشاء حساب مجاني
3. الحصول على API Key المجاني
4. إضافة المفتاح إلى `.env`

---

## 🚀 التشغيل المحلي

### التثبيت الأولي:
```bash
# تثبيت المكتبات المطلوبة
pip install python-telegram-bot requests python-dotenv pytz

# أو استخدام requirements.txt
pip install -r requirements.txt
```

### اختبار النظام:
```bash
# اختبار سريع للمكونات
python app.py test

# تشغيل النظام العادي
python app.py

# عرض المساعدة
python app.py help
```

### التحقق من نجاح التثبيت:
```
✅ تم تحميل متغيرات البيئة من .env
🧪 اختبار سريع للمكونات...

1️⃣ اختبار FarmLogic:
   ✅ تم تحميل الإعدادات: 14 شجرة

2️⃣ اختبار جلب الطقس:

3️⃣ اختبار Telegram:
[Telegram] تم تهيئة البوت للتواصل مع YOUR_CHAT_ID
   ✅ تم تهيئة Telegram بنجاح

✅ الاتصال ناجح مع البوت: YOUR_BOT_NAME (@YOUR_BOT_USERNAME)
✅ جميع الاختبارات نجحت!
```

---

## 🔧 تخصيص الإعدادات

### ملف config.json:
```json
{
  "trees_fertilizer_schedule": {
    "your_tree_name": {
      "seasons": ["spring_season", "summer"],
      "fertilizer": "NPK 20-20-20",
      "amount_kg": 2,
      "notes": "تسميد في الصباح الباكر"
    }
  },
  "chicken_schedule": {
    "deworming": {
      "start_date": "2025-01-01",
      "interval_days": 90,
      "drugs_rotation": ["Fenbendazole", "Ivermectin", "Levamisole"]
    }
  },
  "weather": {
    "api_key": "YOUR_API_KEY",
    "city": "Your City",
    "country": "SA"
  },
  "telegram": {
    "bot_token": "YOUR_BOT_TOKEN",
    "chat_id": "YOUR_CHAT_ID"
  }
}
```

---

## 🚀 النشر على GitHub Actions

### الخطوة 1: إعداد Repository
```bash
# إنشاء repository جديد
git init
git add .
git commit -m "Initial farm notifier system"
git branch -M main
git remote add origin YOUR_REPO_URL
git push -u origin main
```

### الخطوة 2: إعداد GitHub Secrets
في repository settings:

1. الذهاب إلى Settings > Secrets and variables > Actions
2. إضافة Secret جديد:
   - `TELEGRAM_BOT_TOKEN`: توكن البوت
   - `TELEGRAM_CHAT_ID`: معرف المحادثة

### الخطوة 3: تحديث workflow
فحص ملف `.github/workflows/notifier.yml` للتأكد من:
- جدولة زمنية صحيحة (يومياً كل 12 ساعة)
- مسارات الملفات صحيحة
- البيئة البرمجية الصحيحة

### تشغيل Actions يدوياً:
```bash
# إذا لم يعمل تلقائياً
git commit --allow-empty -m "Trigger Actions"
git push
```

---

## 🔍 حل المشاكل الشائعة

### مشكلة: "Chat not found"
**السبب**: البوت لم يتم إضافته للمحادثة
**الحل**:
1. إضافة البوت للمحادثة/المجموعة
2. إرسال رسالة تتضمن اسم البوت
3. التحقق من Chat ID

### مشكلة: "python-telegram-bot not found"
**الحل**:
```bash
pip install python-telegram-bot
```

### مشكلة: "Weather API error"
**السبب**: مفتاح API غير صحيح أو انتهت صلاحيته
**الحل**:
1. التحقق من صحة مفتاح API
2. التأكد من صحة مدينة/دولة
3. فحص حدود API المجانية

### مشكلة: "Permission denied" في GitHub Actions
**الحل**:
1. التأكد من أن repository عام
2. فحص إعدادات GitHub Actions
3. التحقق من tokens

### مشكلة: رسائل فارغة
**السبب**: لا توجد مهام مجدولة لليوم
**الحل**:
1. فحص `config.json`
2. تحديث التواريخ والجداول
3. التحقق من الطقس

---

## 📊 مراقبة النظام

### فحص logs:
```bash
# عرض آخر تشغيل
tail -f .last_run

# فحص الأخطاء
python app.py test
```

### تحديث جداول المهمة:
```python
# في config.json، تحديث تواريخ البداية:
"deworming": {
  "start_date": "2025-01-01",  # تحديث التاريخ
  "interval_days": 90
}
```

---

## 📞 الدعم والمساعدة

### تحقق من:
1. ملف `.env` للتأكد من صحة المفاتيح
2. `config.json` للتصميم الصحيح
3. اتصال الإنترنت
4. صلاحيات البوت في المحادثة

### رسائل الأخطاء الشائعة:
- `❌ فشل اختبار الاتصال`: مشكلة في التوكن أو Chat ID
- `⚠️ لا توجد مهام مجدولة`: لا توجد مهام لليوم
- `❌ خطأ في جلب الطقس`: مشكلة في مفتاح API

---

## 🎉 تم!

إذا رأيت رسالة "✅ جميع الاختبارات نجحت!" فهذا يعني أن النظام يعمل بشكل صحيح!

**النظام جاهز للاستخدام الآن** 🚀
