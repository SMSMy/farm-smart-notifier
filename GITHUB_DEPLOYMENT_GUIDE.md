# 🚀 دليل النشر على GitHub Actions

## نظرة عامة

هذا الدليل سيساعدك في نشر نظام التنبيه الذكي للمزرعة على GitHub Actions ليتم تشغيله تلقائياً كل 12 ساعة.

## الخطوة 1: إعداد GitHub Repository

### 1.1 إنشاء Repository جديد

```bash
# انتقل إلى GitHub وأضف repository جديد
# اسم المشروع: farm-smart-notifier
# وصف: نظام تنبيه ذكي لرعاية الدجاج والأشجار
# جعله خاص أو عام حسب تفضيلك
```

### 1.2 رفع المشروع

```bash
# انتقل إلى مجلد المشروع
cd farm-notifier

# إعداد Git
git init
git add .
git commit -m "Initial commit: نظام التنبيه الذكي للمزرعة"
git branch -M main

# ربط بـ GitHub (استبدل YOUR_USERNAME بحسابك)
git remote add origin https://github.com/YOUR_USERNAME/farm-smart-notifier.git

# رفع المشروع
git push -u origin main
```

## الخطوة 2: إعداد GitHub Secrets

### 2.1 الدخول إلى إعدادات Repository

1. اذهب إلى GitHub repository الذي أنشأته
2. انقر على **Settings** (في الشريط العلوي)
3. في القائمة الجانبية انقر على **Secrets and variables**
4. انقر على **Actions**

### 2.2 إضافة Secrets

إضافة الـ 3 secrets التالية:

| اسم Secret            | القيمة                             |
| --------------------- | ---------------------------------- |
| `TELEGRAM_BOT_TOKEN`  | YOUR_TOKEN_HERE                    |
| `TELEGRAM_CHAT_ID`    | `1003443250446`                    |
| `OPENWEATHER_API_KEY` | `dbe40b3b9ff7646fb726a1a1bde13aba` |

**طريقة الإضافة:**

1. انقر على **New repository secret**
2. اكتب اسم الـ secret في خانة "Name"
3. الصق القيمة في خانة "Secret"
4. انقر على **Add secret**

## الخطوة 3: تفعيل GitHub Actions

### 3.1 التحقق من الـ Workflow

1. في repository، انقر على **Actions** (في الشريط العلوي)
2. ستجد workflow باسم "🚜 نظام التنبيه الذكي للمزرعة"
3. انقر عليه لمشاهدة التفاصيل

### 3.2 تشغيل يدوي

- يمكنك تشغيل الـ workflow يدوياً بالنقر على **Run workflow**
- أو انتظر الجدولة التلقائية (كل 12 ساعة)

### 3.3 مراقبة السجلات

في صفحة Actions، يمكنك:

- مشاهدة سجلات كل تشغيل
- تحميل الملفات المرفقة (logs)
- رؤية حالة كل step

## الخطوة 4: جدولة التشغيل

### 4.1 الجدولة التلقائية

الـ workflow محدد للعمل في:

- **الساعة 6:00 صباحاً** (UTC)
- **الساعة 6:00 مساءً** (UTC)

### 4.2 تغيير الجدولة (اختياري)

لتغيير أوقات التشغيل، عدل في ملف `.github/workflows/farm-notifier.yml`:

```yaml
schedule:
  # تشغيل كل 6 ساعات
  - cron: "0 0,6,12,18 * * *"
```

أو تشغيل يومياً في 8 صباحاً:

```yaml
schedule:
  - cron: "0 8 * * *"
```

## الخطوة 5: إضافة البوت للمجموعة

### 5.1 إضافة البوت

1. اذهب إلى تطبيق Telegram
2. ابحث عن `@DadFarmBot`
3. انقر على إضافة للمجموعة/القناة
4. اختر المجموعة بالمعرف `1003443250446`

### 5.2 منح الصلاحيات

- تأكد من إعطاء البوت صلاحيات إرسال الرسائل
- البوت يجب أن يكون مشرفاً في المجموعة إذا كانت خاصة

## الخطوة 6: الاختبار والمراقبة

### 6.1 اختبار يدوي

```bash
# في GitHub Actions، انقر على "Run workflow"
```

### 6.2 مراقبة السجلات

- اذهب إلى **Actions** في GitHub repository
- انقر على آخر تشغيل
- راجع سجلات كل خطوة

### 6.3 التحقق من الرسائل

- تأكد من استلام الرسائل في Telegram
- تحقق من أن الرسائل تحتوي على:
  - المهام المطلوبة 🐔🔄 🧽✨ 💊🌡️ 🦠💧 🌳🌱
  - نص التوثيق بالعربية والبنغالية 🎥
  - الصور المناسبة لكل مهمة

## استكشاف الأخطاء

### مشكلة: الرسائل لا تصل

1. **تحقق من البوت:** تأكد من إضافة `@DadFarmBot` للمجموعة
2. **تحقق من Chat ID:** تأكد أن `1003443250446` صحيح
3. **تحقق من Secrets:** تأكد من صحة جميع الـ secrets

### مشكلة: خطأ في API

1. **تحقق من مفتاح Weather:** تأكد من صحة `OPENWEATHER_API_KEY`
2. **تحقق من الحدود:** قد تكون تجاوزت حدود API المجانية

### مشكلة: الأخطاء في السجلات

```bash
# في GitHub Actions
1. انقر على "Actions"
2. انقر على آخر تشغيل فاشل
3. انقر على "farm-notification" job
4. راجع السجلات التفصيلية
```

## التحديث والصيانة

### تحديث الكود

```bash
# بعد أي تحديث في الكود
git add .
git commit -m "Update: وصف التحديث"
git push
```

### إضافة ميزات جديدة

1. عدّل الملفات المطلوبة
2. احرص على اختبار محلياً أولاً
3. ارفع التحديثات
4. راقب السجلات للتأكد من عمل كل شيء

## الخلاصة

بعد اتباع هذه الخطوات:
✅ سيتم تشغيل النظام تلقائياً كل 12 ساعة
✅ ستحصل على رسائل Telegram مع المهام المطلوبة
✅ الرسائل ستتضمن الإيموجيات والنصوص المطلوبة
✅ يمكنك مراقبة حالة التشغيل من GitHub Actions
✅ يمكن التحكم في الجدولة والتشغيل يدوياً

## الدعم

إذا واجهت أي مشاكل:

1. راجع سجلات GitHub Actions
2. تأكد من صحة جميع الـ secrets
3. تأكد من إضافة البوت للمجموعة
4. تحقق من صحة Chat ID

**استمتع بإدارة مزرعتك الذكية! 🚜📱**
